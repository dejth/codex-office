# v0.1.2 Validation Record

Status: Published; engineering and maintainer Marketplace gates passed

This patch separates runtime-probed persisted inventory from version-verified
Shared observer status and rewrites the Marketplace README for users. The
maintainer manually published the reviewed package on 2026-07-30; agents did
not upload or publish the Marketplace package.

## Supported and tested versions

- Declared minimum VS Code: `^1.96.0`
- VS Code tested: `1.131.0` on macOS arm64
- Codex schema-verified Shared observer: `0.146.0`
- Persisted inventory: canonical Codex runtime fingerprints whose consumed
  responses pass strict bounded runtime schemas
- Local version evidence: CLI `0.146.0`; ChatGPT bundle
  `0.146.0-alpha.3.1` retained as an unverified prerelease test case
- Marketplace target: `dejth.codex-office` version `0.1.2`

## Deterministic gates

```bash
pnpm check
pnpm audit --prod
pnpm exec vsce ls
pnpm exec vsce package --out artifacts/codex-office-0.1.2.vsix
node scripts/check-budgets.mjs \
  --dist dist \
  --vsix artifacts/codex-office-0.1.2.vsix
node scripts/smoke-clean-profile.mjs \
  --vsix artifacts/codex-office-0.1.2.vsix \
  --baseline-vsix artifacts/codex-office-0.1.1.vsix
shasum -a 256 artifacts/codex-office-0.1.2.vsix
```

## Release evidence

No evidence may contain real Codex sessions, prompts, usernames, workspace
paths, profile contents, account identifiers, or token values.

| Gate                            | Result | Evidence                                                                                                    |
| ------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------- |
| Capability and provider tests   | Pass   | 3 files / 61 focused tests; verified, future, prerelease, malformed, strict-data, and shared fallback cases |
| `pnpm check`                    | Pass   | 25 test files / 231 tests; typecheck, lint, format, build, and dist budget pass                             |
| Production dependency audit     | Pass   | `pnpm audit --prod`: no known vulnerabilities                                                               |
| Marketplace metadata and README | Pass   | Manifest 0.1.2 plus packaged setup, compatibility, live-status, troubleshooting, and privacy sections       |
| Package contents and budget     | Pass   | 24 files / 689,212 bytes; SHA-256 `cba7515bcc0c73470a88baf2e278a498100d9f01c2f774f2624b6d8df9757d0f`        |
| Clean-profile lifecycle         | Pass   | Install, list, disable, uninstall, and absence pass on VS Code 1.131.0                                      |
| Upgrade from 0.1.1              | Pass   | Genuine 0.1.1 VSIX upgraded to 0.1.2 in an isolated profile                                                 |
| Installed lifecycle             | Pass   | Local VSIX reports 0.1.2; Extension Host activation and verified Shared observer connection completed       |
| Marketplace publication         | Pass   | Maintainer published `dejth.codex-office` 0.1.2 manually; public Gallery API reported 0.1.2 on 2026-07-30   |

## Rollback

If runtime-probed inventory creates a privacy, activation, or compatibility
regression, prepare another reviewed patch and use the maintainer-controlled
Marketplace deprecation or removal controls if the published build creates an
urgent privacy or security risk.
