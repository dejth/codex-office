# v0.1 Validation Record

Status: 0.1.0 beta engineering and compatibility gates verified; human
Marketplace gates remain pending

This record defines reproducible release evidence. It does not authorize
publication, tagging, pushing, or Marketplace mutation.

## Supported and tested versions

- Declared minimum VS Code: `^1.96.0`
- Current VS Code tested: `1.130.0` on macOS arm64
- Codex CLI live-status evidence version: `0.145.0`

The candidate passed the isolated lifecycle on both the declared-minimum line
and current stable. This does not replace the human sidebar walkthrough or
Marketplace review.

## Deterministic gates

```bash
pnpm check
pnpm audit --prod
pnpm exec vsce package --out artifacts/codex-office-0.1.0.vsix
node scripts/check-budgets.mjs \
  --dist dist \
  --vsix artifacts/codex-office-0.1.0.vsix
node scripts/smoke-clean-profile.mjs \
  --vsix artifacts/codex-office-0.1.0.vsix
shasum -a 256 artifacts/codex-office-0.1.0.vsix
```

The budget check enforces these candidate ceilings:

| Artifact                 |       Maximum |
| ------------------------ | ------------: |
| Extension host bundle    |       140 KiB |
| Webview JavaScript       |       350 KiB |
| Webview stylesheet       |        20 KiB |
| Nine emitted status PNGs | 600,000 bytes |
| Complete `dist` tree     |         1 MiB |
| VSIX                     |       700 KiB |

The VSIX inspection rejects TypeScript, source maps, tests, tasks, docs, raw
generation art, and production source PNG duplicates. Its JSON output records
the candidate SHA-256 checksum.

## Clean-profile lifecycle

`smoke-clean-profile.mjs` creates unique temporary user-data and extension
directories. It never reads or writes the normal VS Code profile. The script
installs the candidate, verifies its ID/version, exercises the process-scoped
disable option, uninstalls it, verifies absence, and removes the temporary
profile.

Upgrade evidence requires a genuine older baseline VSIX. Without one, upgrade
remains explicitly untested rather than simulated by reinstalling the same
version.

The command-line smoke does not claim automated visual interaction with a
running sidebar. Activation, rendering, focus, themes, zoom, and reduced motion
remain separate test and human-review evidence.

## Candidate evidence

No evidence below contains real Codex sessions, prompts, usernames, workspace
paths, profile contents, or account usage.

| Gate                        | Environment              | Result  | Evidence                                                                                             |
| --------------------------- | ------------------------ | ------- | ---------------------------------------------------------------------------------------------------- |
| `pnpm check`                | macOS arm64              | Pass    | 25 test files / 222 tests                                                                            |
| Production dependency audit | pnpm lockfile            | Pass    | `pnpm audit --prod`: no known vulnerabilities                                                        |
| Budget/package audit        | VSIX 0.1.0               | Pass    | 687,755 bytes / 24 files; SHA-256 `5bb70d0db956e0e47f681c622932f711ff40797fd20c6cab2fa23e1d4f131fe5` |
| VS Code current stable      | 1.130.0 macOS arm64      | Pass    | Isolated install, list, disable option, uninstall, absence                                           |
| VS Code declared minimum    | 1.96.4 macOS arm64       | Pass    | Official archive; isolated install, list, disable option, uninstall, absence                         |
| Upgrade from older VSIX     | No genuine baseline      | Pending | Candidate install works; released-version upgrade is not simulated                                   |
| Codex live status           | CLI 0.145.0              | Pass    | Owner-only Shared observer lifecycle verified against accepted ADR-0005                              |
| Accessibility matrix        | Deterministic source/DOM | Pass    | Keyboard, focus/text cues, theme tokens, narrow layout, and reduced-motion tests                     |
| Listing visual              | Synthetic production UI  | Pass    | Sanitized fixture; no prompt, path, chat, account identity, or real usage data                       |

The candidate VSIX contains 24 allowlisted files. It excludes tests, tasks,
source, scripts, development configuration, release artifacts, raw atlas
files, source PNG duplicates, and source maps.

The maintainer has reviewed the current installed sidebar. The sanitized
listing visual and final candidate remain part of the release review, and
publication still requires separate explicit approval.

## Rollback

Uninstall `dejth.codex-office`. Before a first public release there is no prior
Marketplace build to restore, so rollback means removing or deprecating the
listing and publishing a corrected patch only after review. Publication
rollback remains a manual maintainer action under CDD-007.
