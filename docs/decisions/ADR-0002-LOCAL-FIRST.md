# ADR-0002 — Local-first and telemetry-free

Status: Accepted

The extension performs normal operation locally and sends no analytics or session data. This reduces privacy risk and earns trust for a tool that observes sensitive agent activity. Any future networked feature is opt-in and requires a new ADR, threat-model update, and explicit maintainer approval.
